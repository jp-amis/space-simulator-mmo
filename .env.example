# Copy to .env for local overrides (loaded by mise). Never commit real secrets.
NODE_ENV=development

# Server
PORT=8080

# Client (Vite) — must be prefixed with VITE_ to be exposed to the browser
VITE_WS_URL=ws://localhost:8080/ws
